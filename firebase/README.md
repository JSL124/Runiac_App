## Purpose
Reserves the future Firebase backend project area for Runiac.

## Future contents
Firebase CLI configuration, Cloud Functions, Firestore rules, Storage rules, emulator setup, and messaging contracts.

## Scaffold-only note
No `firebase init` output, Firebase config, rules, Functions package, or generated backend source exists yet.
