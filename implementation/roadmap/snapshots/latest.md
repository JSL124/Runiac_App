# Runiac Latest Roadmap Snapshot

## Snapshot Metadata

- Last updated phase: Phase 01 - Governance CI
- Last updated capsule: Run launch map UI implementation closure; most recent completed roadmap capsule document remains `implementation/roadmap/capsules/leaderboard-leagues-popup-shell.md`
- Latest verified commit: `08c51c7 feat(run): add floating plan launch map UI`
- Latest Android smoke evidence checkpoint: `d63a775 docs(roadmap): record android smoke test evidence`
- Latest roadmap checkpoint: `f7e1625 docs(roadmap): route frontend hygiene cleanup capsule`
- Latest implementation completion: `08c51c7 feat(run): add floating plan launch map UI`
- Latest ready-for-commit implementation: none.
- Routing commit: `e2a96ed docs(roadmap): route flutter app shell capsule`
- Closure context: Phase 01 Governance CI is closed at `f917aab`; Artifact Inventory Schema persistence is complete; Repository Workflow Record capsule is closed; workflow memory schema migration, historical isolation check repair, historical isolation runner integration, Governance CI scaffold-baseline transition, Flutter scaffold baseline, static Flutter app shell baseline, and post-shell static UI/nav alignment checkpoint are pushed and verified; `flutter-app-shell-baseline` is closed; `android-ui-smoke-test-evidence` validation evidence is complete; `home-dashboard-visual-polish` is closed after static Home dashboard visual polish validation; `premium-home-dashboard-static-wireframe-alignment` is closed after static Premium Home Dashboard wireframe alignment; `github-actions-governance-ci-baseline` is closed after adding the minimal GitHub Actions governance workflow; `home-dashboard-scroll-layout-stability-fix` is closed after stabilizing Home dashboard scroll/card layout; `run-tab-static-placeholder` is closed after adding the static RunLandingPage-style Run tab placeholder; `run-tab-fullscreen-map-overlay-alignment` is closed after static Run tab fullscreen map overlay layout alignment; `home-dashboard-reference-layout-alignment` is closed after static Home dashboard reference layout alignment; `maps-tab-static-placeholder` is committed and pushed at `323507b`; `run-launch-fullscreen-static-interaction` is committed and pushed at `b5c31ef`; Run launch back-handling cleanup is committed and pushed at `5851057`; `home-dashboard-primary-action-simplification` is committed and pushed at `9254faa`, with Home brand/action color follow-up committed and pushed at `bd2963d`; `run-launch-brand-color-polish` is committed and pushed at `e1f9c6d`; `maps-static-discovery-hierarchy-polish` is committed and pushed at `60fe96f` and closed at `0725e61`; `leaderboard-static-motivation-hierarchy-polish` is superseded before implementation due to the refined map-first Leaderboard direction; `leaderboard-map-first-landing-shell` is implemented at `b1ed742` and closed; `github-actions-flutter-validation-baseline` is closed after workflow implementation commit `587cc0e`; hosted GitHub Actions for `587cc0e` was manually confirmed PASS by the user; CI parity is complete; `leaderboard-help-modal-shell` is implemented at `96a2706` and closed at `2d1ec46` after manually confirmed hosted GitHub Actions PASS for `96a2706`; inspect-only Flutter frontend hygiene audit is complete; `flutter-frontend-hygiene-cleanup` is implemented at `8074092` and closed after local validation plus manually confirmed hosted GitHub Actions PASS for `8074092`; `leaderboard-region-preview-sheet-shell` is closed at `09d6389` after hosted GitHub Actions Governance CI #41 PASS; `leaderboard-leagues-popup-shell` is closed at `e1d8b74` after hosted GitHub Actions Governance CI #42 PASS; Run launch map UI is closed at `08c51c7` after local validation and hosted GitHub Actions Governance CI #43 PASS. No active implementation capsule is selected.

## Current Implementation State

Approved Flutter scaffold baseline has been generated, committed, and pushed under `implementation/mobile/runiac_app/` with:

```bash
flutter create --template=app --platforms=android,ios --org com.runiac --project-name runiac_app --no-pub implementation/mobile/runiac_app
```

No `flutter pub get`, `firebase init`, `flutterfire configure`, build, deploy, or Firebase setup command was run. Worktree inspection found no Firebase project config, `firebase.json`, `.firebaserc`, `firebase_options.dart`, `google-services.json`, `GoogleService-Info.plist`, committed `.env*`, service account, or credentials file. The repository remains Firebase-uninitialized.

The repository is now in scaffold-baseline state, not pre-scaffold state. The stock counter template has been replaced by a static offline Runiac app shell, and the current bottom navigation is Home, Maps, Run, Leaderboard, and You. The post-shell static UI/nav alignment checkpoint at `247b4e5 feat(mobile): align static Runiac nav baseline` updated only `implementation/mobile/runiac_app/lib/app.dart` and `implementation/mobile/runiac_app/test/widget_test.dart`. This shell baseline, alignment checkpoint, and closed Home visual polish capsule do not authorize Phase 02 feature work, Firebase setup, dependency installation, builds, deploys, further custom production tests, or additional Runiac production source implementation outside a future routed capsule.

The `flutter-app-shell-baseline` capsule is closed at `e48a348 feat(mobile): add static Runiac app shell`. The latest verified pushed implementation checkpoint is `247b4e5 feat(mobile): align static Runiac nav baseline`. The latest Android smoke evidence checkpoint is `d63a775 docs(roadmap): record android smoke test evidence`; the `android-ui-smoke-test-evidence` validation-only capsule verified the current static Flutter UI baseline on Android emulator `emulator-5554`.

The `home-dashboard-visual-polish` capsule is closed. Capsule type: Flutter UI product-code-changing. Required chain completed: `A0_ORCH -> A9_TRACE -> A5_WIRE -> A10_FLUTTER_IMPL -> A6_REVIEW -> A12_QA_TEST -> A8_OUTPUT_CHECKER`. The implementation improved the static Home dashboard spacing, visual hierarchy, Runiac logo palette baseline, `Start Run` CTA presentation, supportive guidance card, and static empty-state copy. Bottom navigation remains Home / Maps / Run / Leaderboard / You. The app remains static UI only; Phase 02 is unselected, Firebase setup is not authorized, backend behavior was not added, and fake backend-owned values remain forbidden.

The `premium-home-dashboard-static-wireframe-alignment` capsule is closed. Capsule type: Flutter UI product-code-changing. Required chain completed: `A0_ORCH -> A9_TRACE -> A5_WIRE -> A10_FLUTTER_IMPL -> A6_REVIEW -> A12_QA_TEST -> A8_OUTPUT_CHECKER`. The implementation aligns the static Home dashboard to the provided Premium Home Dashboard wireframe structure only: top greeting/header, Today's Plan, Goal Preparation, Runner Progress, This Week's Plan, Last Run, Advice, and Recommended Community Route. Bottom navigation remains Home / Maps / Run / Leaderboard / You. The app remains static UI only; Phase 02 is unselected, Firebase setup is not authorized, backend behavior was not added, and fake backend-owned values remain forbidden.

The `github-actions-governance-ci-baseline` capsule is closed. Capsule type: CI/governance documentation + workflow. Required chain completed: `A0_ORCH -> A9_TRACE -> A6_REVIEW -> A12_QA_TEST -> A8_OUTPUT_CHECKER`. The workflow at `.github/workflows/governance-ci.yml` runs on push and pull request events targeting `main`, uses `ubuntu-latest`, checks out the repository, runs `git diff --check`, and runs `./tools/governance-ci/run-all-checks.sh`. It does not add Flutter SDK setup, `flutter pub get`, Flutter analyze/test in Actions, Android/iOS build, Firebase, secrets, environment variables, deployment, product code changes, Phase 02 selection, or a product implementation capsule. GitHub-hosted Actions pass/fail status is not claimed in local closure evidence and requires post-push hosted run inspection.

The `github-actions-flutter-validation-baseline` capsule is closed. Routing commit: `95d1eed docs(roadmap): route flutter validation ci capsule`. Workflow implementation commit: `587cc0e ci: add flutter validation to governance workflow`. Local validation before workflow commit passed: `git diff --check`, `./tools/governance-ci/run-all-checks.sh`, `cd implementation/mobile/runiac_app && flutter analyze --no-pub`, and `cd implementation/mobile/runiac_app && flutter test`. Hosted GitHub Actions for `587cc0e` was manually confirmed PASS by the user. The hosted workflow now runs `git diff --check`, `./tools/governance-ci/run-all-checks.sh`, Flutter SDK setup, `flutter pub get`, `flutter analyze --no-pub`, and `flutter test`. This closure does not authorize Firebase, init, deploy, native release builds, dependency changes, product implementation, or Phase 02 selection.

The `home-dashboard-scroll-layout-stability-fix` capsule is closed. Capsule type: Flutter UI bugfix. Required chain completed: `A0_ORCH -> A9_TRACE -> A5_WIRE -> A10_FLUTTER_IMPL -> A6_REVIEW -> A12_QA_TEST -> A8_OUTPUT_CHECKER`. The implementation disables Home dashboard overscroll stretch, keeps clamping scroll physics, and makes the route placeholder explicitly full-width with its existing fixed height. It stabilizes card and placeholder constraints during scrolling without redesigning the screen, adding sections, changing product meaning, or adding fake backend-owned values. The app remains static UI only; Phase 02 is unselected.

The `run-tab-static-placeholder` capsule is closed. Capsule type: Flutter UI product-code-changing. Required chain completed: `A0_ORCH -> A9_TRACE -> A5_WIRE -> A10_FLUTTER_IMPL -> A6_REVIEW -> A12_QA_TEST -> A8_OUTPUT_CHECKER`. Implemented scope is a static RunLandingPage-style Run tab placeholder with a large map/route-looking visual area, static route line, marker/flag placeholders, Today's Plan card, Setting button, central Start button, and Switch Route button. The app remains static UI only; Phase 02 is unselected; Firebase setup is not authorized; no GPS/tracking, real map integration, timer, distance, pace, heart-rate, cadence, activity recording, activity submission, run result, XP/streak/level/rank, leaderboard score, route data, premium state, backend-owned values, dependency changes, or native Android/iOS changes were introduced.

The `run-tab-fullscreen-map-overlay-alignment` capsule is closed. Capsule type: Flutter UI layout-alignment. Required chain completed: `A0_ORCH -> A5_WIRE -> A10_FLUTTER_IMPL -> A6_REVIEW -> A12_QA_TEST -> A8_OUTPUT_CHECKER`. Implemented scope removed the visible setup-later helper card, kept the fullscreen map-like background, preserved the floating Today's Plan card, and positioned Setting / Start / Switch Route below the card and above bottom navigation. Validation before closure: `flutter analyze --no-pub` PASS; `flutter test` PASS; `git diff --check` PASS; `./tools/governance-ci/run-all-checks.sh` PASS; Android smoke evidence PASS on `emulator-5554`. The app remains static UI only; Phase 02 is unselected; Firebase setup is not authorized; no GPS/tracking, real map integration, timer, distance, pace, heart-rate, cadence, activity recording, activity submission, run result, XP/streak/level/rank, leaderboard score, route data, premium state, backend-owned values, dependency changes, or native Android/iOS changes were introduced.

The `home-dashboard-reference-layout-alignment` capsule is closed. Capsule type: Flutter UI layout-alignment. Required chain completed: `A0_ORCH -> A9_TRACE -> A5_WIRE -> A10_FLUTTER_IMPL -> A6_REVIEW -> A12_QA_TEST -> A8_OUTPUT_CHECKER`. Implemented scope is static Home dashboard reference layout alignment only: used the provided wireframe as structural reference, preserved safe placeholder copy, and aligned the Home card order to Greeting / Today's Plan / Training Goal / Runner Progress / This Week's Plan / Last Run / Post-run Feedback / Recommended Routes. Validation before closure: `flutter analyze --no-pub` PASS; `flutter test` PASS; `git diff --check` PASS; `./tools/governance-ci/run-all-checks.sh` PASS; Android smoke evidence PASS on `emulator-5554`. The app remains static UI only; Phase 02 is unselected; Firebase setup is not authorized; no GPS/tracking, real map integration, timer, distance, pace, heart-rate, cadence, activity recording, activity submission, run result, fake AI advice, fake premium entitlement, XP/streak/level/rank, leaderboard score, route data, premium state, backend-owned values, dependency changes, or native Android/iOS changes were introduced.

The `home-dashboard-primary-action-simplification` capsule is closed after static frontend-only Home primary action hierarchy and brand color polish. Completion commits are `9254faa feat(mobile): polish home primary action hierarchy` and `bd2963d feat(mobile): polish home brand action hierarchy`. The implementation kept profile and notification controls inert, kept View Plan secondary, made Quick Start the primary action, removed competing secondary buttons from This Week's Plan and Last Run, and applied a Home-only blue/orange brand color hierarchy. The app remains static UI only; Phase 02 is unselected; Firebase setup is not authorized; no Firebase/Auth/Firestore/Cloud Functions, GPS/location behavior, real run tracking, timers, real plan generation, XP/streak/level/rank/leaderboard computation or mutation, premium/subscription logic, backend-like data behavior, dependency changes, native Android/iOS changes, or unrelated screen changes were introduced.

The `run-launch-brand-color-polish` capsule is closed after static frontend-only Run launch visual color polish. Completion commit is `e1f9c6d feat(mobile): polish run launch brand colors`. The implementation made Start orange/action-oriented, kept the route line blue, kept Setting and Route setup as secondary white/blue pill actions, kept Today's Plan calm and readable, and preserved static/inert behavior. Validation passed: `git diff --check`, `flutter analyze --no-pub`, `flutter test`, and `./tools/governance-ci/run-all-checks.sh`. The app remains static UI only; Phase 02 is unselected; Firebase setup is not authorized; no GPS/location behavior, run tracking, timers, route setup logic, fake run metrics, XP/streak/level/rank/leaderboard computation or mutation, backend-like data behavior, dependency changes, native Android/iOS changes, shell navigation changes, Home changes, Maps changes, or unrelated screen changes were introduced.

The Run launch map UI implementation is closed at `08c51c7 feat(run): add floating plan launch map UI` after local `flutter analyze`, `flutter test`, `./tools/governance-ci/run-all-checks.sh`, and hosted GitHub Actions Governance CI #43 PASS for commit `08c51c7b25bcdfcf8415ffcfe45a2f01a38cae9b`. Implemented scope is Run launch UI only: full-screen map-style background, top-left X button, top-center GPS ready pill, top-right settings gear button, shared X/settings circular tap feedback, static runner marker, floating Today's Plan / Start Run card, and widget test updates. Required boundary preserved: bottom navigation shell unchanged; no Home, Maps, Leaderboard, or You/Profile changes; no Firebase/backend/GPS/timer/dependency changes; no XP, streak, level, rank, leaderboard score, weekly XP, monthly XP, subscription privilege state, or expert plan publication state mutation. The next milestone requires explicit next capsule selection.

The `maps-static-discovery-hierarchy-polish` capsule is closed after static frontend-only Maps visual hierarchy polish. Completion commit is `60fe96f feat(mobile): polish maps discovery hierarchy`. The implementation improved static route discovery hierarchy, search/Saved balance, map marker/route accents, route card hierarchy, and the Shared Routes draggable sheet. The bottom sheet remains draggable, collapses to a handle-only state, removes the subtitle/helper/Preview badge, and expands to at most about half the screen. Validation passed: `git diff --check`, `flutter analyze --no-pub`, `flutter test`, and `./tools/governance-ci/run-all-checks.sh`. The app remains static UI only; Phase 02 is unselected; Firebase setup is not authorized; no GPS/location behavior, real map SDK, real map tiles, route generation, route recommendation logic, route persistence, saved-route behavior, fake route metrics, XP/streak/level/rank/leaderboard computation or mutation, backend-like data behavior, dependency changes, native Android/iOS changes, shell navigation changes, Home changes, Run changes, Leaderboard changes, You/Profile changes, or unrelated screen changes were introduced.

The `leaderboard-static-motivation-hierarchy-polish` capsule is superseded before implementation because the desired Leaderboard direction changed from static motivation hierarchy polish to a map-first, region-based landing direction. It is not treated as completed implementation and has no implementation completion commit target.

The `leaderboard-map-first-landing-shell` capsule is closed after implementation-approved static frontend-only Leaderboard map-first landing shell work. Completion commit is `b1ed742 feat(mobile): add leaderboard map landing shell`. Implemented scope is a static map-first Leaderboard landing shell with full-screen map-like background, static `Weekly XP / Monthly XP` overlay, static current league selector overlay, visual info button, highlighted user area, no bottom sheet visible by default, and the smallest necessary Leaderboard-only AppBar suppression through `runiac_shell.dart`. Validation before commit passed: `git diff --check`, `flutter analyze --no-pub`, `flutter test`, and `./tools/governance-ci/run-all-checks.sh`. The app remains static UI only; Phase 02 is unselected; Firebase setup is not authorized; no GPS/current-location behavior, real map SDK, real map tiles, real leaderboard data, leaderboard aggregation, help modal behavior, region tap behavior, region preview bottom sheet, internal ranking preview, fake users, fake ranks, fake XP, fake scores, fake streaks, fake levels, XP/streak/level/rank/leaderboard score/weekly XP/monthly XP mutation, subscription privilege state, expert plan publication state, dependencies, native Android/iOS changes, scaffold, build, init, deploy, or unrelated screen edits were introduced.

No dirty Flutter files remain after the `leaderboard-map-first-landing-shell` implementation commit; final implementation status was clean and aligned with `origin/main`.

The `leaderboard-help-modal-shell` capsule is closed at `96a2706 feat(mobile): add leaderboard tips popup` after manually confirmed hosted GitHub Actions PASS for `96a2706`. Implemented scope: the existing `Leaderboard information` affordance opens a centered Tips popup/dialog with concise beginner-friendly help content for Leagues, Weekly vs Monthly, and Ranking readiness; close behavior is implemented; widget tests cover open/content/dismiss and absence of forbidden fake leaderboard content. Validation before commit passed: `git diff --check`, `flutter analyze --no-pub`, `flutter test`, and `./tools/governance-ci/run-all-checks.sh`. The app remains static UI only; Phase 02 is unselected; Firebase setup is not authorized; no league selector popup, region tap behavior, region preview bottom sheet, fake leaderboard rows, fake users, fake ranks, fake XP, fake scores, fake levels, fake streaks, real leaderboard data, leaderboard aggregation, XP/rank/score calculation or mutation, weekly/monthly XP mutation, Firebase/Auth/Firestore/Cloud Functions/FCM, GPS/location, native/dependency changes, shell/navigation/AppBar changes, Home/Maps/Run/You changes, scaffold/build/init/deploy, or workflow changes were introduced.

The `flutter-frontend-hygiene-cleanup` capsule is closed at `8074092 chore(mobile): apply frontend hygiene cleanup` after local validation and manually confirmed hosted GitHub Actions PASS for `8074092`. Implemented scope: removed the inactive RunTab shell body path from `RuniacShell` while preserving Run launch route behavior; flattened the redundant `RunControls` wrapper; added safe const constructors/usages for small custom painters; and added a stable widget-test assertion that `Tips` is absent before tapping `Leaderboard information`.

The cleanup intentionally skipped broad refactors, inert placeholder behavior changes, league selector popup behavior, region tap behavior, region preview sheet behavior, `leaderboard_tab.dart` splitting, and color-system normalization. It did not authorize new UX features, fake leaderboard users/ranks/XP/scores, backend-owned value mutation, Firebase/Auth/Firestore/Cloud Functions/FCM, GPS/location behavior, native Android/iOS changes, dependency or `pubspec.yaml` changes, workflow changes, broad visual redesign, broad architecture refactor, roadmap reactivation, next-capsule routing, Phase 02 selection, scaffold, build, init, deploy, Firebase setup, or `flutterfire configure`.

The `leaderboard-region-preview-sheet-shell` capsule is closed at `09d6389 feat(mobile): add draggable leaderboard region preview sheet` after hosted GitHub Actions Governance CI #41 PASS. Implemented scope: a static Leaderboard region preview bottom sheet shell with safe display-only placeholders, inert visual CTA labels, local draggable expanded/collapsed sheet behavior, map/ranked-area tap expansion, widget-test coverage for safe labels, drag handle/tap interaction, and forbidden copied/fake content absence.

The `leaderboard-leagues-popup-shell` capsule is closed at `e1d8b74 feat(mobile): add leaderboard leagues popup` after hosted GitHub Actions Governance CI #42 PASS. Implemented scope: preserved the existing info-icon Tips popup and added a separate static Leagues taxonomy popup from the `Rising Runner Division` / `Lv.11 - Lv.20` league pill area. The popup uses a rounded white dialog with close X, centered `Leagues` title, grey medal icons, and display-only league taxonomy rows. Local validation before commit passed: `git diff --check`, `flutter analyze --no-pub`, `flutter test`, and `./tools/governance-ci/run-all-checks.sh`. No active implementation capsule is selected.

The `maps-tab-static-placeholder` capsule is ready for commit. Capsule type: Flutter UI product-code-changing. Required chain completed: `A0_ORCH -> A9_TRACE -> A5_WIRE -> A10_FLUTTER_IMPL -> A6_REVIEW -> A12_QA_TEST -> A8_OUTPUT_CHECKER`. Implemented scope is static Maps tab placeholder UI only: replaced the generic Maps placeholder with a MapsLandingPage-style structure using a full map-like background, decorative roads/route-like shapes, marker placeholders, top search bar, primary-blue Saved pill control, and draggable bottom shared route panel with safe placeholder cards. Validation before ready-for-commit: `flutter analyze --no-pub` PASS; `flutter test` PASS; `git diff --check` PASS; `./tools/governance-ci/run-all-checks.sh` PASS; Android smoke evidence PASS on `emulator-5554`. The app remains static UI only; Phase 02 is unselected; Firebase setup is not authorized; no GPS/location permission, current location state, real map SDK, route generation, route recommendation logic, saved-route persistence, fake route/place/distance/duration/difficulty/rating/saved-count/community data, XP/streak/level/rank, leaderboard score, premium state, backend-owned values, dependency changes, native Android/iOS changes, or workflow configuration changes were introduced. This capsule is not staged, committed, or pushed.

The `flutter-source-structure-refactor` capsule is ready for commit. Capsule type: Flutter source-structure refactor. Required chain completed: `A0_ORCH -> A9_TRACE -> A10_FLUTTER_IMPL -> A6_REVIEW -> A12_QA_TEST -> A8_OUTPUT_CHECKER`. Implemented scope is behavior-preserving source structure only: `app.dart` is now a thin app composition file, theme/colors live under `lib/core/theme/`, shared visual primitives live under `lib/core/widgets/`, shell navigation lives under `lib/features/shell/`, and the currently implemented Home, Maps, Run, Leaderboard, and You UI lives under feature presentation folders. Bottom navigation remains Home / Maps / Run / Leaderboard / You. Plan remains a future independent feature reachable from You; no Plan bottom-navigation tab, empty Plan folder, or Plan navigation was introduced. Validation before ready-for-commit: `flutter analyze --no-pub` PASS; `flutter test` PASS; `git diff --check` PASS; `./tools/governance-ci/run-all-checks.sh` PASS; Android smoke evidence PASS on `emulator-5554`. The app remains static UI only; Phase 02 is unselected; Firebase setup is not authorized; no Firebase/Auth/Firestore/Cloud Functions, GPS/location behavior, real map SDK, route generation, route persistence, run tracking, activity submission, backend-owned values, dependency changes, native Android/iOS changes, or workflow configuration changes were introduced. This capsule is not staged, committed, or pushed.

The `run-controls-and-plan-spacing-polish` capsule is ready for commit. Capsule type: Flutter static UI polish. Required chain completed: `A0_ORCH -> A9_TRACE -> A5_WIRE -> A10_FLUTTER_IMPL -> A6_REVIEW -> A12_QA_TEST -> A8_OUTPUT_CHECKER`. Implemented scope is static Run tab spacing/control polish only: grouped Today’s Plan and Setting / Start / Switch Route controls into one bottom overlay column, increased bottom clearance from the shell-owned bottom navigation, capped overlay width, added compact spacing for narrow widths, kept Start visually primary, and kept Setting / Switch Route as static secondary controls with safer label fit. Validation before ready-for-commit: `flutter analyze --no-pub` PASS after one const fix; `flutter test` PASS; `git diff --check` PASS; `./tools/governance-ci/run-all-checks.sh` PASS; Android smoke evidence PASS on `emulator-5554` with screenshot at `/private/tmp/runiac-run-controls-spacing-polish.png`. The app remains static UI only; Phase 02 is unselected; Firebase setup is not authorized; no GPS/location permission, current location state, real map SDK, route generation, route persistence, run tracking, timer, distance, pace, duration, heart-rate, cadence, activity submission, backend-owned values, dependency changes, native Android/iOS changes, GitHub Actions workflow changes, or unrelated screen changes were introduced. This capsule is not staged, committed, or pushed.

The `run-launch-fullscreen-static-interaction` capsule is ready for commit. Capsule type: Flutter static UI interaction. Required chain completed: `A0_ORCH -> A14_ERROR_TRIAGE -> A9_TRACE -> A5_WIRE -> A10_FLUTTER_IMPL -> A6_REVIEW -> A12_QA_TEST -> A8_OUTPUT_CHECKER`. Implemented scope is static Run launch interaction only: the Run bottom-navigation item opens an opaque full-screen slide-up Run launch route, the launch route hides the shell bottom navigation while open, top-left Close and Android back dismiss to the previous shell state, the Today’s Plan card floats separately over the map-like background, and Setting / Start / Route setup float as a separate control row. Validation before ready-for-commit: `flutter analyze --no-pub` PASS; `flutter test` PASS; `git diff --check` PASS; `./tools/governance-ci/run-all-checks.sh` PASS; Android smoke screenshot skipped by user preference to reduce iteration time. The app remains static UI only; Phase 02 is unselected; Firebase setup is not authorized; no Firebase/Auth/Firestore/Cloud Functions, GPS/location permission, real map SDK, route generation, route persistence, run tracking, timer, distance, pace, duration, heart-rate, cadence, calories, activity submission, XP/streak/level/rank/leaderboard, premium entitlement, dependency changes, native Android/iOS changes, GitHub Actions workflow changes, or unrelated screen redesign were introduced. This capsule is not staged, committed, or pushed.

## Implemented

- Governance CI check contract committed at `implementation/roadmap/ci/governance-ci-check-contract.md`.
- Local Governance CI shell checks committed at `tools/governance-ci/` in `50be93e chore(ci): add local governance CI checks`.
- Governance CI routing hardening committed in `7ed7f27 chore(ci): harden governance CI routing`.
- `tools/governance-ci/check-roadmap-routing.sh` is generalized and `tools/governance-ci/run-all-checks.sh` is available as the local runner.
- Governance CI runner passed for Phase 01 closure review.
- Artifact Inventory Schema persisted at `docs/meta/ARTIFACT_INVENTORY_SCHEMA.md` in `7aaacf1 docs(meta): add artifact inventory schema`.
- Artifact Inventory Schema remains non-operational and schema-only.
- Documentation scope instructions committed in `bbdde20 docs(agents): add documentation scope instructions`.
- Repository Workflow Record created at `docs/meta/REPOSITORY_WORKFLOW_RECORD.md` in `04e0972 docs(roadmap): route repository workflow record`.
- Workflow memory checkpoints pushed in `0eb37c8 docs(meta): add workflow memory checkpoints`.
- Workflow Memory Drift Check pushed in `93fff5e ci(governance): add workflow memory drift check`.
- Workflow memory schema migration pushed in `9f2c832 docs(meta): standardize workflow memory schema`.
- `docs/meta/REPOSITORY_WORKFLOW_RECORD.md` now uses the standardized 7-field workflow memory checkpoint schema and deterministic confidence labels.
- Historical isolation check narrowed in `0619874 ci(governance): narrow historical isolation check` to allow legitimate `docs/meta` non-operational boundary references while preserving failure behavior for operational authority/dependency usage.
- Main Governance CI runner includes historical isolation coverage as of `6d65fa1 ci(governance): include historical isolation check`.
- Local Governance CI has been transitioned to allow the approved Flutter scaffold baseline under `implementation/mobile/runiac_app/` while continuing to block Firebase config, secrets, service accounts, credentials, signing material, Cloud Functions, Firestore rules, Storage rules, build/deploy artifacts, and unauthorized scaffold paths.
- Governance CI scaffold-baseline transition committed in `c8b2942 ci(governance): allow approved Flutter scaffold baseline`.
- Flutter scaffold baseline committed in `4b375d2 chore(mobile): add Flutter scaffold baseline`.
- Static Flutter app shell baseline committed in `e48a348 feat(mobile): add static Runiac app shell`.
- `flutter-app-shell-baseline` capsule is closed after A6_REVIEW PASS, A8_OUTPUT_CHECKER PASS, `flutter analyze --no-pub` PASS, and Governance CI PASS.
- Static shell scope is limited to five placeholder tabs: Home, Plan, Run, Explore, and Leaderboard.
- No Firebase, GPS, authentication, leaderboard, XP, or backend-owned logic was added by the shell baseline.
- Static UI/nav alignment checkpoint committed in `247b4e5 feat(mobile): align static Runiac nav baseline`.
- Checkpoint affected files: `implementation/mobile/runiac_app/lib/app.dart`; `implementation/mobile/runiac_app/test/widget_test.dart`.
- Validation before push for `247b4e5`: `flutter analyze` PASS; `flutter test` PASS; `./tools/governance-ci/run-all-checks.sh` PASS; `git diff --check` PASS; scope review PASS.
- `247b4e5` is static UI/nav alignment only and did not start Firebase, GPS, authentication, Firestore, leaderboard, plan, profile, XP, streak, level, rank, premium-state, subscription privilege state, expert plan publication state, or backend-owned logic.
- Workflow Memory Drift Check is detection-only, WARN-only local Governance CI support. It does not automatically update workflow memory, snapshots, CURRENT.md, or capsules.
- Repository Workflow Record capsule is closed.
- `flutter-app-shell-baseline` is closed.
- `android-ui-smoke-test-evidence` was routed as a validation-only capsule to verify the current static Flutter UI baseline on Android emulator `emulator-5554`.
- Expected Android smoke-test evidence: emulator detected as `emulator-5554`, app launches successfully, bottom navigation visible with Home / Maps / Run / Leaderboard / You, no runtime crash observed, and command outputs recorded.
- Android smoke-test evidence completed: `flutter devices` detected `emulator-5554`; `flutter analyze --no-pub` passed; `flutter test` passed; `flutter run -d emulator-5554` launched the app with no runtime crash observed in console output.
- Visual evidence completed: temporary screenshot at `/private/tmp/runiac-android-smoke.png` showed the app launched with bottom navigation Home / Maps / Run / Leaderboard / You; old Plan / Explore bottom-navigation labels were not visible; screenshot is not committed.
- App remained static UI only during smoke testing; no Firebase, GPS, authentication, Firestore, backend behavior, XP, streak, level, rank, leaderboard score, premium-state, or backend-owned logic was observed.
- `home-dashboard-visual-polish` was routed as a Flutter UI product-code-changing capsule in `51aa504 docs(roadmap): route home dashboard visual polish capsule`.
- Required `home-dashboard-visual-polish` agent chain completed: `A0_ORCH -> A9_TRACE -> A5_WIRE -> A10_FLUTTER_IMPL -> A6_REVIEW -> A12_QA_TEST -> A8_OUTPUT_CHECKER`.
- Implemented scope is Home dashboard visual polish only: spacing, typography hierarchy, Runiac logo palette baseline, card/button visual structure, Start Run CTA presentation, beginner-friendly guidance text, static empty/supportive copy, and preservation of bottom navigation order Home / Maps / Run / Leaderboard / You.
- Scope remained static UI only and forbade Firebase, Auth, Firestore, Cloud Functions, GPS/tracking, real run recording, fake XP, fake streak, fake level, fake rank, fake leaderboard score, fake premium state, fake run history, backend-owned values, dependency changes, and native Android/iOS changes.
- `home-dashboard-visual-polish` is closed after static Home dashboard visual polish implementation.
- Completion target: `feat(mobile): polish static home dashboard`.
- Modified implementation file: `implementation/mobile/runiac_app/lib/app.dart`.
- Widget test file did not require changes because the existing navigation-label assertions remained valid.
- Validation before closure: `flutter analyze --no-pub` PASS; `flutter test` PASS; `git diff --check` PASS; `./tools/governance-ci/run-all-checks.sh` PASS.
- Android smoke evidence after polish: `flutter devices` detected `emulator-5554`; `flutter run -d emulator-5554` launched successfully with no runtime crash observed in console output; screenshot evidence at `/private/tmp/runiac-home-polish-smoke.png` showed polished Home UI using the Runiac blue/orange/white palette and bottom navigation Home / Maps / Run / Leaderboard / You.
- The app remained static UI only; no Firebase, GPS, authentication, Firestore, Cloud Functions, real run recording, XP, streak, level, rank, leaderboard score, premium-state, run history, backend-owned values, dependency changes, native Android/iOS changes, or Phase 02 selection was introduced.
- `premium-home-dashboard-static-wireframe-alignment` was routed and closed as a Flutter UI product-code-changing capsule.
- Required `premium-home-dashboard-static-wireframe-alignment` agent chain completed: `A0_ORCH -> A9_TRACE -> A5_WIRE -> A10_FLUTTER_IMPL -> A6_REVIEW -> A12_QA_TEST -> A8_OUTPUT_CHECKER`.
- Implemented scope is Home dashboard static wireframe alignment only: top greeting/header, Today's Plan card, Goal Preparation placeholder, Runner Progress placeholder, This Week's Plan skeleton, Last Run placeholder, Advice placeholder, Recommended Community Route placeholder, and preservation of bottom navigation order Home / Maps / Run / Leaderboard / You.
- Scope remained static UI only and forbade Firebase, Auth, Firestore, Cloud Functions, GPS/tracking, real run recording, premium entitlement logic, AI advice logic, fake XP, fake streak, fake level, fake rank, fake leaderboard score, fake weekly/monthly XP, fake subscription state, fake premium state, fake run history, fake route data, backend-owned values, dependency changes, and native Android/iOS changes.
- Validation before closure: first `flutter analyze --no-pub` PASS; first `flutter test` failed due an unconstrained skeleton placeholder layout assertion; after a bounded skeleton sizing fix, second `flutter analyze --no-pub` PASS; second `flutter test` PASS; `git diff --check` PASS; `./tools/governance-ci/run-all-checks.sh` PASS.
- Governance CI does not run `flutter test`, so Flutter test was run separately. The second Flutter test run was necessary because the first run failed and a fix was applied.
- Android smoke evidence after alignment: `flutter devices` detected `emulator-5554`; `flutter run -d emulator-5554` launched successfully with no runtime crash observed in console output; screenshot evidence at `/private/tmp/runiac-premium-home-smoke.png` showed the static Home dashboard using the Runiac blue/orange/white palette, placeholder-only sections, and bottom navigation Home / Maps / Run / Leaderboard / You.
- The app remained static UI only; no Firebase, GPS, authentication, Firestore, Cloud Functions, real run recording, premium entitlement logic, AI advice logic, XP, streak, level, rank, leaderboard score, weekly/monthly XP, premium-state, run history, fake route data, backend-owned values, dependency changes, native Android/iOS changes, or Phase 02 selection was introduced.
- `github-actions-governance-ci-baseline` was routed as a CI/governance-only capsule to add a minimal GitHub Actions workflow that runs existing governance checks on push and pull request events targeting `main`.
- Its allowed workflow scope is limited to checkout, `git diff --check`, and `./tools/governance-ci/run-all-checks.sh`.
- It does not add Flutter SDK setup, dependency resolution, Android/iOS build, Firebase, secrets, deployment, environment variables, product code changes, Phase 02 selection, or a product implementation capsule.
- Governance CI exact allowlist was updated only for `.github/workflows/governance-ci.yml` and `implementation/roadmap/capsules/github-actions-governance-ci-baseline.md`.
- Validation before closure: `git diff --check` PASS; `./tools/governance-ci/run-all-checks.sh` PASS; local YAML inspection PASS.
- GitHub-hosted Actions pass/fail status remains pending until a real post-push run is inspected.
- Hosted GitHub Actions Governance CI for commit `336f0d0 ci: add governance checks workflow` passed after push. Run `26445235864` completed successfully on `main`; job `governance-ci` and steps `Checkout repository`, `Check diff whitespace`, and `Run governance checks` all concluded success.
- `home-dashboard-scroll-layout-stability-fix` was routed as a Flutter UI bugfix capsule for Home dashboard scroll/card constraint stability.
- It does not authorize a redesign, new Home sections, product meaning changes, fake backend-owned values, Firebase, Auth, Firestore, Cloud Functions, GPS/tracking, backend integration, dependency changes, native platform changes, or Phase 02 selection.
- `home-dashboard-scroll-layout-stability-fix` is closed after stabilizing the Home dashboard scroll layout.
- Implemented scope: `ScrollConfiguration.of(context).copyWith(overscroll: false)`, `ClampingScrollPhysics`, and explicit full-width constraint for the fixed-height Recommended Community Route placeholder.
- Validation before closure: `flutter analyze --no-pub` PASS; `flutter test` PASS; `git diff --check` PASS; `./tools/governance-ci/run-all-checks.sh` PASS.
- Android smoke evidence after bugfix: `flutter devices` detected `emulator-5554`; `flutter run -d emulator-5554` launched successfully with no runtime crash observed in console output; screenshots at `/private/tmp/runiac-scroll-stability-top.png`, `/private/tmp/runiac-scroll-stability-lower.png`, and `/private/tmp/runiac-scroll-stability-return.png` showed lower Home cards and the route placeholder remained bounded and stable with bottom navigation Home / Maps / Run / Leaderboard / You.
- The app remained static UI only; no Firebase, GPS, authentication, Firestore, Cloud Functions, real run recording, premium entitlement logic, AI advice logic, XP, streak, level, rank, leaderboard score, weekly/monthly XP, premium-state, run history, fake route data, backend-owned values, dependency changes, native Android/iOS changes, or Phase 02 selection was introduced.
- `run-tab-static-placeholder` is closed after static Run tab placeholder implementation.
- Required `run-tab-static-placeholder` agent chain completed: `A0_ORCH -> A9_TRACE -> A5_WIRE -> A10_FLUTTER_IMPL -> A6_REVIEW -> A12_QA_TEST -> A8_OUTPUT_CHECKER`.
- Implemented scope is static UI only: large route/map visual placeholder, static route line, visual marker/flag placeholders, Today's Plan card with safe empty-state copy, Setting, Start, and Switch Route controls, and preserved bottom navigation Home / Maps / Run / Leaderboard / You.
- Validation before closure: `flutter analyze --no-pub` PASS; `flutter test` PASS; `git diff --check` PASS; `./tools/governance-ci/run-all-checks.sh` PASS.
- Governance CI does not run `flutter test`, so Flutter test was run separately exactly once.
- Android smoke evidence after Run placeholder: `flutter devices` detected `emulator-5554`; `flutter run -d emulator-5554` launched successfully with no runtime crash observed in console output; screenshot evidence at `/private/tmp/runiac-run-placeholder-smoke.png` showed the static Run tab placeholder, route/map visual, Today's Plan card, Setting, Start, Switch Route, setup-later copy, and bottom navigation Home / Maps / Run / Leaderboard / You.
- The Start button smoke tap produced no navigation change, no tracking transition, and no new console output.
- The app remained static UI only; no Firebase, GPS, authentication, Firestore, Cloud Functions, real map integration, timer, distance, pace, heart-rate, cadence, calories, activity recording, activity submission, run result, XP, streak, level, rank, leaderboard score, route data, premium-state, backend-owned values, dependency changes, native Android/iOS changes, or Phase 02 selection was introduced.

## Not Implemented

- Firebase project/config setup.
- Firestore rules or collections.
- Cloud Functions source.
- Production app source code beyond the static offline Flutter app shell baseline.
- Production tests/build/deploy pipeline.
- Artifact inventory entries.
- Repository history reconstruction.
- Timelines, Genesis material, retrospectives, or autonomous archive systems.

## Current Architecture Assumptions

- Flutter handles UI, navigation, GPS tracking UI, and local interaction.
- Firebase Authentication handles identity.
- Firestore stores users, plans, activities, routes, XP summaries, and leaderboard data.
- Cloud Functions own XP calculation, activity validation, streak update, level update, rank update, and leaderboard aggregation.

## Current Governance Assumptions

- `implementation/traceability/setup-gates.md` is the source of truth for setup-gate approval state.
- Gate-00 is `APPROVED`.
- Flutter Scaffold Gate was separately human-approved for the bounded scaffold execution. The generated scaffold baseline does not authorize Firebase setup or Phase 02 work beyond the scaffold baseline.
- Firebase Project and Config Gate is `Not Started`.
- Secret / API Key / Environment Handling Gate is `Not Started`.
- Human/project approval evidence is required before remaining setup gates become `Approved`.
- Root roadmap context protocol is active in `AGENTS.md`.
- `docs/pdd/diagrams/` is the canonical PDD diagram source; root `diagrams/` is legacy/reference-only.
- Claude deny rules and `.gitignore` cover nested `.env`, Firebase config, service account, signing, private GPS/location/route, and test-evidence artifact patterns.
- Operational authority remains with `implementation/roadmap/CURRENT.md`, active phase/capsule documents, ADRs, and validated snapshots.
- `docs/meta` remains non-operational and must not override operational-authority sources.
- `docs/meta` remains non-operational and is not approval evidence, routing authority, setup-gate authority, or implementation guidance.
- Repository Workflow Record work is closed and remains documentation/governance-only. It does not authorize implementation, setup, scaffold, source, test, build, deploy, or init work.
- Workflow Memory Drift Check warnings are detection-only and do not create approval, closure, snapshot refresh, or workflow-memory update authority.
- Static Flutter app shell closure does not authorize Firebase setup, Phase 02 routing, further source expansion, dependency installation, build, deploy, init, or backend work.
- Backend-owned values remain protected: XP, streak, level, rank, leaderboard score, weekly XP, monthly XP, subscription privilege state, and expert plan publication state.
- Safe Visible Product Acceleration is allowed for future explicitly routed frontend prototype capsules only: one visible screen-level improvement per capsule, static Flutter UI, placeholder display data, existing widgets/dependencies, small widget test updates, and minimal capsule/snapshot updates. It does not authorize Firebase/Auth/Firestore/Cloud Functions, `flutterfire configure`, Google Maps or Mapbox SDK integration, GPS/native configuration, new dependencies without explicit approval, client-side mutation or calculation of backend-owned values, XP/streak/level/rank/leaderboard score/weekly XP/monthly XP/subscription privilege state/expert plan publication state logic, unrelated refactors, roadmap expansion, or new ADRs unless a real architecture decision is unavoidable.
- Android UI smoke-test evidence closure does not authorize product code edits, Flutter source/test edits, iOS/CocoaPods work, Firebase setup, GPS/tracking, authentication, Firestore, leaderboard logic, XP/streak/level/rank, premium-state, or backend-owned logic.
- `home-dashboard-visual-polish` is closed and does not authorize further Flutter implementation. It does not select Phase 02 and does not authorize Firebase, backend, dependency, native, GPS/tracking, Auth, Firestore, or fake backend-owned state changes.
- `premium-home-dashboard-static-wireframe-alignment` is closed and does not authorize further Flutter implementation. It does not select Phase 02 and does not authorize Firebase, backend, dependency, native, GPS/tracking, Auth, Firestore, premium entitlement logic, AI advice logic, or fake backend-owned state changes.
- `github-actions-governance-ci-baseline` is closed and remains CI/governance-only. It does not imply Flutter, Firebase, backend, production, deployment, branch protection, or GitHub-hosted run readiness beyond the minimal workflow definition and local validation evidence.
- `github-actions-flutter-validation-baseline` is closed and remains CI workflow validation-only. It added hosted Flutter SDK setup plus `flutter analyze --no-pub` and `flutter test` while preserving governance checks. It does not authorize Flutter source changes, product feature implementation, Firebase/Auth/Firestore/Cloud Functions/FCM, Firebase init/deploy, secrets/config/environment setup, Android/iOS native release builds, dependency/package changes, or Phase 02 selection.
- `home-dashboard-scroll-layout-stability-fix` is closed and does not authorize further Flutter implementation. It preserves static UI-only behavior, backend-owned value boundaries, bottom navigation order, and Phase 02 unselected state.
- `home-dashboard-reference-layout-alignment` is closed and does not authorize further Flutter implementation. It preserves static UI-only behavior, backend-owned value boundaries, bottom navigation order, and Phase 02 unselected state.
- `run-tab-static-placeholder` is closed and does not authorize further Flutter implementation. It preserves static UI-only behavior, backend-owned value boundaries, bottom navigation order, and Phase 02 unselected state.
- `run-tab-fullscreen-map-overlay-alignment` is closed and does not authorize further Flutter implementation. It preserves static UI-only behavior, backend-owned value boundaries, bottom navigation order, and Phase 02 unselected state.
- `run-controls-and-plan-spacing-polish` is ready for commit after static Run tab spacing/control polish. It does not authorize Phase 02 selection, Firebase, Auth, Firestore, Cloud Functions, GPS/location, real map SDK, route generation, route persistence, run tracking, timer, distance, pace, duration, heart-rate, cadence, activity submission, XP/streak/level/rank/leaderboard, premium entitlement, dependencies, native platform changes, GitHub Actions workflow changes, or unrelated screen edits.
- `run-launch-fullscreen-static-interaction` is committed and pushed at `b5c31ef`, with follow-up cleanup pushed at `5851057`. It does not authorize Phase 02 selection, Firebase, Auth, Firestore, Cloud Functions, GPS/location, real map SDK, route generation, route persistence, run tracking, timer, distance, pace, duration, heart-rate, cadence, calories, activity submission, XP/streak/level/rank/leaderboard, premium entitlement, dependencies, native platform changes, GitHub Actions workflow changes, or unrelated screen edits.
- `home-dashboard-primary-action-simplification` is closed at `9254faa` with Home brand/action color follow-up closed at `bd2963d`. It preserved static frontend-only behavior and did not authorize Phase 02 selection, Firebase, Auth, Firestore, Cloud Functions, GPS/location, real run tracking, timers, real plan generation, XP/streak/level/rank/leaderboard, premium/subscription logic, backend-like data behavior, dependencies, native platform changes, shell navigation changes, or unrelated screen edits.
- `run-launch-brand-color-polish` is closed at `e1f9c6d`. It preserved static frontend-only behavior and did not authorize Phase 02 selection, Firebase, Auth, Firestore, Cloud Functions, GPS/location, real run tracking, timers, route setup logic, fake run metrics, XP/streak/level/rank/leaderboard, premium/subscription logic, backend-like data behavior, dependencies, native platform changes, shell navigation changes, Home changes, Maps changes, or unrelated screen edits.
- `maps-static-discovery-hierarchy-polish` is closed at `60fe96f`. It preserved static frontend-only behavior and did not authorize Phase 02 selection, Firebase, Auth, Firestore, Cloud Functions, GPS/location, real map SDK, real map tiles, route generation, route recommendation logic, route persistence, saved-route behavior, fake route metrics, XP/streak/level/rank/leaderboard, premium/subscription logic, backend-like data behavior, dependencies, native platform changes, shell navigation changes, Home changes, Run changes, Leaderboard changes, You/Profile changes, or unrelated screen edits.
- `leaderboard-static-motivation-hierarchy-polish` is superseded before implementation and is not treated as completed implementation.
- `leaderboard-map-first-landing-shell` is closed at `b1ed742`. It preserved static frontend-only behavior and did not authorize Phase 02 selection, Firebase, backend aggregation, GPS/current-location, real map SDK, real leaderboard data, help modal behavior, region tap behavior, region preview bottom sheet, fake users, fake ranks, fake XP, fake scores, fake streaks, fake levels, backend-owned value mutation, shell navigation redesign, bottom navigation changes, route architecture changes, dependencies, native platform changes, or unrelated screen edits.

## Known Limitations

- Snapshot contains only compressed confirmed state.
- It does not replace `CURRENT.md`, active phase documents, active capsules, or ADRs.
- It must be updated when active phase, capsule, gate status, or implementation state changes.

## Current Active Milestone

Phase 01 - Governance CI is closed at `f917aab`. Artifact Inventory Schema persistence is complete at `7aaacf1`. Repository Workflow Record capsule is closed after `04e0972`, `0eb37c8`, `93fff5e`, schema refresh commit `9f2c832`, historical isolation repair commit `0619874`, and historical isolation runner integration commit `6d65fa1`. Governance CI scaffold-baseline transition is committed at `c8b2942`; Flutter scaffold baseline is committed and pushed at `4b375d2`; static Flutter app shell baseline is committed and pushed at `e48a348`; post-shell static UI/nav alignment checkpoint is committed and pushed at `247b4e5`; latest Android smoke evidence checkpoint is `d63a775`; home dashboard visual polish routing checkpoint is `51aa504`. `flutter-app-shell-baseline` is closed. `android-ui-smoke-test-evidence` is closed after successful Android smoke-test evidence. `home-dashboard-visual-polish` is closed after successful Flutter UI Capsule Chain review, validation, and Android smoke evidence. `premium-home-dashboard-static-wireframe-alignment` is closed after successful Flutter UI Capsule Chain review, validation, and Android smoke evidence. `github-actions-governance-ci-baseline` is closed after adding the minimal GitHub Actions governance workflow, and hosted Governance CI run `26445235864` passed for commit `336f0d0`. `github-actions-flutter-validation-baseline` is routed at `95d1eed`, implemented at `587cc0e`, and closed after local validation and manually confirmed hosted GitHub Actions PASS for `587cc0e`; CI parity is complete. `home-dashboard-scroll-layout-stability-fix` is closed after successful Flutter UI Capsule Chain review, validation, and Android smoke evidence. `run-tab-static-placeholder` is closed after successful Flutter UI Capsule Chain review, validation, and Android smoke evidence. `run-tab-fullscreen-map-overlay-alignment` is closed after successful Flutter UI Capsule Chain review, validation, and Android smoke evidence. `home-dashboard-reference-layout-alignment` is closed after successful Flutter UI Capsule Chain review, validation, and Android smoke evidence. `maps-tab-static-placeholder` is committed and pushed at `323507b`. `flutter-source-structure-refactor` is ready for commit after successful behavior-preserving source structure refactor validation and Android smoke evidence. `run-controls-and-plan-spacing-polish` is committed and pushed at `08bca0f`. `run-launch-fullscreen-static-interaction` is committed and pushed at `b5c31ef`, with follow-up cleanup committed and pushed at `5851057`. `home-dashboard-primary-action-simplification` is committed and pushed at `9254faa`, with Home brand/action color follow-up committed and pushed at `bd2963d`. `run-launch-brand-color-polish` is committed and pushed at `e1f9c6d`. `maps-static-discovery-hierarchy-polish` is committed and pushed at `60fe96f` and closed at `0725e61`. `leaderboard-static-motivation-hierarchy-polish` is superseded before implementation. `leaderboard-map-first-landing-shell` is committed and pushed at `b1ed742` and closed. `leaderboard-help-modal-shell` is committed and pushed at `96a2706` and closed at `2d1ec46` after local validation plus manually confirmed hosted GitHub Actions PASS. `flutter-frontend-hygiene-cleanup` is committed and pushed at `8074092` and closed after local validation plus manually confirmed hosted GitHub Actions PASS. `leaderboard-region-preview-sheet-shell` is committed at `09d6389` and closed after hosted GitHub Actions Governance CI #41 PASS. `leaderboard-leagues-popup-shell` is committed at `e1d8b74` and closed after hosted GitHub Actions Governance CI #42 PASS. Run launch map UI is committed at `08c51c7` and closed after hosted GitHub Actions Governance CI #43 PASS. Current active milestone: post-Run-launch-map-UI closure planning gate with no active implementation capsule selected. Phase 02 implementation, Firebase setup, `flutterfire configure`, dependency installation, build, init, deploy, backend work, native release build workflow, and production implementation outside a selected capsule remain unauthorized until separate explicit approval exists.
