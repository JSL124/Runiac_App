## Final Recommendation

Accept Claude’s feedback overall and revise the plan before any implementation work. The original plan is directionally sound, but it should be tightened around the frozen submitted PDD baseline, manual commit protocol, scaffold churn controls, MVP scope, and current untracked traceability artifacts.

No files were modified, staged, committed, built, tested, initialized, or deployed in this review.

## Claude Feedback Decision Table

| Claude Feedback | Decision | Reason | Final Action |
| --- | --- | --- | --- |
| Read/extract the frozen submitted PDD before writing traceability | Accept | `docs/submissions/pdd/README.md` says the PDF is the frozen baseline. `pdftotext` and `pdfinfo` are unavailable locally, so submitted-PDD alignment is currently unverified. | Make submitted-PDD extraction/delta recording the first read-only prerequisite. |
| Add explicit manual commit gate | Accept | Root `AGENTS.md` requires no auto-commit unless explicitly allowed; traceability docs are not AGENTS cleanup. | Stop at Ready for commit, run A6_REVIEW and A8_OUTPUT_CHECKER, provide scoped manual git commands only. |
| Constrain `flutter create` churn | Accept | Current `.gitignore` only contains `.DS_Store`; no Flutter app exists. PRD targets iOS and Android. | Later use `flutter create --platforms android,ios runiac_app`, add Flutter ignores before staging, review generated tree. |
| Constrain `firebase init` churn and project binding | Accept | `firebase/AGENTS.md` says no production Firebase config exists and Firebase files require explicit implementation work. | Later use emulator-first setup only, no deploy, no production project, add Functions ignores, pin runtime. |
| Defer Phase-2 expert/admin-governance fixtures from Sprint 0 | Accept with scope note | `docs/pdd/04-class-diagram.md` places `MedicalTrainerExpert`, `ExpertPlan`, `PlanReview`, leaderboard, routes, and AI summary as Phase 2 or premium extensions, but root AGENTS makes governance rules non-negotiable. | Keep governance as default-deny/traceability constraints in Phase 1; defer full fixtures until feature phase. |
| Fix affected-vs-untouched path contradiction | Accept | Same paths were listed as future affected and currently untouched. | Label paths by batch: untouched now, created only in later approved batch. |
| Avoid staging empty review stubs | Accept with correction | Current empty file is `implementation/traceability/decisions/2026-05-23T13-38-25_codex_decision.md`, not under `reviews/`. | Do not stage empty artifacts; remove only if cleanup is explicitly approved. |
| Add default-deny rules posture | Accept | Firebase/security rules require trusted backend enforcement and no client admin paths. | Document default-deny intent in setup gates; do not create production rules until approved. |
| Add secrets/key handling constraint | Accept | Maps and LLM/API keys are relevant future risks. | State no secrets, API keys, map keys, or LLM keys may be committed. |
| Carry privacy constraints into emulator/fixtures | Accept | PRD §6.2 and `firebase/AGENTS.md` require GPS, health, and activity data protection. | Use only synthetic, non-precise route/location data in future fixtures/logs/evidence. |
| Pin versions | Accept | PRD risk R12 calls out dependency breakage and lockfiles. | Require Flutter SDK and Functions Node runtime decisions before scaffold commits. |
| Reconcile role naming drift | Accept | PRD uses “Trainer / Medical Advisor”; AGENTS/PDD use “Medical Trainer/Expert”. | Traceability must annotate the terminology mapping instead of silently normalizing it. |
| Defer map-provider decision as Sprint 0 blocker | Accept | Maps matter later for route/leaderboard features; not required for traceability-only setup. | Keep map provider as later scaffold/feature gate, not first-task blocker. |
| Reduce traceability doc count | Accept | Four setup docs may duplicate AGENTS/PDD constraints. | Start with `requirements-map.md` and `setup-gates.md`; add more only if needed. |

## Revised Final Plan

1. Start with read-only baseline verification: extract or otherwise obtain text from the submitted PDD PDF, compare it against `docs/pdd/`, and record any deltas relevant to implementation traceability.

2. First writable task, only after approval: create a minimal traceability/setup batch under `implementation/traceability/`, focused on MVP features F1, F2, F3, F4, F6, and F9.

3. Keep Phase 2 features F5, F7, F8, F10, expert-plan governance fixtures, leaderboard fixtures, route sharing, AI summaries, and map-provider implementation out of Sprint 0 except as deferred/default-deny constraints.

4. Before any Flutter scaffold: confirm package ID, platforms, Flutter SDK, target path absence, and `.gitignore`; then use `flutter create --platforms android,ios runiac_app` only after separate approval.

5. Before any Firebase scaffold: confirm demo/emulator project, environment naming, region, emulator targets, Functions runtime, rules-test approach, `.gitignore`, and no deploy/no production project.

6. Stop at Ready for commit for all non-AGENTS changes. Run A6_REVIEW and A8_OUTPUT_CHECKER before readiness, then provide manual scoped `git add` commands only.

## Files To Modify If Approved

- `implementation/traceability/requirements-map.md`
- `implementation/traceability/setup-gates.md`
- Optional later cleanup: `implementation/traceability/decisions/2026-05-23T13-38-25_codex_decision.md`
- Later scaffold batch only: `.gitignore`
- Later Flutter batch only: `implementation/mobile/runiac_app/`
- Later Firebase batch only: files under `firebase/`
- Later test batch only: files under `tests/firebase-rules/`, `tests/functions-integration/`, and `tests/fixtures/`

## Files To Keep Untouched

- `PRD.md`
- `docs/pdd/`
- `docs/submissions/pdd/`
- `docs/submissions/pdd/FYP-26-S2-38_Project_Design_Document_submitted_2026-05-23.pdf`
- Existing diagrams and wireframes
- `implementation/mobile/runiac_app/` until Flutter scaffold approval
- `firebase/functions/`, `firebase/firestore/`, `firebase/storage/` until Firebase scaffold approval
- Production source code
- GitHub Actions workflows
- Firestore and Storage rules until explicit Firebase/rules approval

## Implementation Prompt For Later

Use A0_ORCH as workflow owner. Use A9_TRACE before implementation. First, obtain readable text from the frozen submitted PDD PDF and compare it with `docs/pdd/`; document relevant deltas. Then create only `implementation/traceability/requirements-map.md` and `implementation/traceability/setup-gates.md` for MVP features F1, F2, F3, F4, F6, and F9. Include backend ownership, subscription/userRole separation, privacy constraints, default-deny posture for deferred collections, and manual approval gates for Flutter/Firebase scaffolding. Do not scaffold Flutter or Firebase. Do not run builds/tests/init/deploy. Run A6_REVIEW and A8_OUTPUT_CHECKER, then stop at Ready for commit with scoped manual git commands.

## User Approval Required

Approve whether the next task is traceability-only setup.

Provide or approve a method to read/extract the submitted PDD PDF, since local `pdftotext` and `pdfinfo` are unavailable.

Confirm whether to remove the empty untracked decision stub later, or leave it unstaged.

Confirm that Sprint 0 should exclude Flutter scaffold, Firebase scaffold, production rules, tests, deploys, and feature implementation.
