## Final Recommendation

Accept Claude’s feedback and tighten the original plan before any implementation.

The revised next step should be smaller: resolve the pending profile migration gate first, then add only a clearly labelled manual/no-automated-review path if still needed. Defer lite/full review modes, review-packet generation, `DECISION_POLICY=always|if-needed`, and Codex-as-reviewer unless the user explicitly approves that tradeoff later.

Current git state has three untracked traceability artifacts from this workflow run:

```text
implementation/traceability/decisions/2026-05-23T14-20-53_codex_decision.md
implementation/traceability/plans/2026-05-23T14-20-53_codex_plan.md
implementation/traceability/reviews/2026-05-23T14-20-53_claude_review.md
```

They should not be staged with a tooling implementation commit.

## Claude Feedback Decision Table

| Claude Feedback | Decision | Reason | Final Action |
| --- | --- | --- | --- |
| Resolve profile migration sequencing before efficiency work. | Accept | Existing decision `13-56-50` already gates profile migration, and both plans touch `run_plan_review.sh`, README, and prompts. | Make sequencing a hard approval gate: migrate first, or explicitly defer migration before minimal efficiency work. |
| Full config matrix is overengineered. | Accept | README says wait for 3-5 real tasks before externalizing; current workflow is still early and single-repo. | Do not implement 27 combinations. Keep next batch minimal. |
| `REVIEW_PROVIDER=codex` weakens independent review. | Accept | The workflow purpose is Codex plan -> Claude review -> Codex decision. Codex reviewing Codex removes that cross-model check. | Omit Codex provider from the next implementation unless separately approved with explicit warnings. |
| `DECISION_POLICY=always` is dangerous wording near implementation gate. | Accept | The runner currently guarantees implementation is separate and user-approved; “always” could be misread as auto-proceed. | Do not add `DECISION_POLICY=always` or `if-needed` now. Preserve manual decision only. |
| Manual checklist mode can be mistaken for automated review. | Accept | Review artifacts currently use `*_claude_review.md`; a manual artifact needs a distinct identity. | If added, use a clearly named `*_manual_checklist.md`, label it “not an automated review,” and use overwrite protection. |
| Keep Runiac rules out of generic runner. | Accept | README explicitly says `runner/` must not include Runiac PRD/PDD/business rules. | Put checklist content in Runiac prompts/profile files; runner only routes and writes guarded outputs. |
| Lite review may weaken PDD/submitted-baseline drift checking. | Accept | Claude prompt and `CLAUDE.md` require access to `docs/submissions/pdd/` and repo context. | Defer lite/full modes and packet generation. If added later, retain `Read,Grep,Glob` and submitted-PDD reachability. |
| Preserve Claude guardrails in all modes. | Accept | Current runner uses `--permission-mode plan`, `--tools "Read,Grep,Glob"`, and `--append-system-prompt "$(cat CLAUDE.md)"`. | Keep these unchanged for any Claude path. |
| Default new vars safely under `set -u`. | Accept | Runner uses `set -euo pipefail`; missing sourced config values must not break existing configs. | Use `${VAR:-default}` after sourcing config. |
| Define provider/profile precedence. | Accept | Prior migration decision requires deterministic precedence; current runner only has `AGENT_REVIEW_CONFIG`. | Document and implement precedence if profile/config changes are approved. |
| README must document manual mode and any loss of independent review. | Accept | README owns workflow safety semantics. | Add safety docs if manual mode or alternate providers are introduced. |
| Existing traceability file was mislabeled unrelated. | Accept | Current git state shows plan/review/decision artifacts from this workflow, not unrelated user work. | Reword as workflow traceability artifacts that should remain unstaged for tooling commits. |
| Use `chore(agent-review): ...` commit scope. | Accept | Git history for this area consistently uses `chore(agent-review): ...`. | Use `chore(agent-review): add configurable manual review mode` or similar. |

## Revised Final Plan

1. Do not implement yet.

2. First choose sequencing:
   - Preferred: complete the approved profile migration first.
   - Alternative: explicitly defer profile migration and apply only a minimal change to the current layout.

3. Minimal efficiency batch only:
   - Keep current Claude review as the default and primary review path.
   - Optionally add `REVIEW_PROVIDER=none` for a manual checklist artifact.
   - Do not add Codex reviewer, lite/full modes, packet generator, or decision automation in this batch.

4. Preserve safety:
   - Plan/decision stay `codex --sandbox read-only --ask-for-approval never`.
   - Claude stays `--permission-mode plan --tools "Read,Grep,Glob" --append-system-prompt "$(cat CLAUDE.md)"`.
   - Implementation remains separate and user-approved.
   - No silent fallback from Claude to another reviewer.

5. Keep layering clean:
   - Runner handles routing, defaults, dry-run behavior, and overwrite safety.
   - Runiac checklist text lives in `prompts/runiac/` or future `profiles/runiac/`.

6. If manual mode is implemented:
   - Write a distinct manual checklist artifact.
   - Make dry-run behavior preview-only.
   - Use `ensure_new_file`.
   - Label output as not an automated review.

## Files To Modify If Approved

If profile migration happens first, modify the equivalent future profile paths instead.

Minimal legacy-layout batch:

```text
tools/agent-review/README.md
tools/agent-review/config/runiac.agent-review.env.example
tools/agent-review/runner/run_plan_review.sh
tools/agent-review/runner/lib/common.sh
tools/agent-review/prompts/runiac/02_claude_review_plan.md
```

Optional only if approved:

```text
tools/agent-review/prompts/runiac/05_manual_review_checklist.md
```

## Files To Keep Untouched

```text
PRD.md
docs/pdd/
docs/submissions/pdd/
docs/submissions/pdd/FYP-26-S2-38_Project_Design_Document_submitted_2026-05-23.pdf
implementation/mobile/
firebase/
tests/
test-evidence/
```

Do not stage current workflow traceability artifacts:

```text
implementation/traceability/decisions/2026-05-23T14-20-53_codex_decision.md
implementation/traceability/plans/2026-05-23T14-20-53_codex_plan.md
implementation/traceability/reviews/2026-05-23T14-20-53_claude_review.md
```

## Implementation Prompt For Later

Implement only the approved minimal `tools/agent-review` efficiency batch. Preserve the existing Claude review path and all safety flags. Add a clearly labelled manual/no-automated-review mode only if approved, with dry-run support and overwrite protection. Keep Runiac-specific checklist content in prompts or the future Runiac profile, not in generic runner logic. Do not add Codex-as-reviewer, lite/full modes, packet generation, or decision automation. Do not modify PRD, PDD, submitted PDF, Flutter, Firebase, tests, or existing traceability artifacts. Do not stage or commit.

## User Approval Required

User approval is required before any implementation. This review was inspect-only: no files were modified, staged, committed, tested, built, deployed, or run through Flutter/Firebase/npm.
