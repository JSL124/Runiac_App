## Goal

Create a compact inspect-only workflow smoke-test plan for the Runiac agent-review workflow, owned by `A0_ORCH`, with no file modifications, no test/build execution, no staging, and no commits.

## Repo Context Checked

- Ran cheap inventory only inside declared allow paths.
- Confirmed representative workflow files exist:
  - `tools/agent-review/README.md`
  - `tools/agent-review/runner/run_plan_review.sh`
  - `.claude/settings.json`
  - `AGENTS.md`
  - `CLAUDE.md`
- Did not scan product, implementation, Firebase, test, PDD, submission, diagram, image, PDF, or generated-asset areas.

## Planning Evidence Read

- Files actually read:
  - `tools/agent-review/README.md`
  - `tools/agent-review/runner/run_plan_review.sh`
  - `.claude/settings.json`

- Files intentionally skipped:
  - `AGENTS.md` and `CLAUDE.md`: known allowed context, but not needed for this compact smoke-test plan.
  - `tools/agent-review` profile prompts/configs beyond representative runner/docs: unnecessary for lite smoke scope.
  - PRD/PDD, `docs/pdd`, `docs/submissions`, Flutter, Firebase, tests, diagrams, wireframes, images, PDFs, generated assets, `implementation/mobile`.

- Reason for skipping large/reference/irrelevant files:
  - The user declared `workflow` context and explicitly prohibited product/implementation/reference expansion.
  - The smoke test only needs representative workflow evidence for context class, plan scope, review scope, lite review, and Claude cap behavior.

## Context Class Decision

- Selected context class: `workflow`
- Reason: The task explicitly targets agent-review workflow behavior, runner controls, review mode, planning/review scope, and Claude cost caps.
- Excluded classes and why:
  - `docs`: no documentation deliverable content is being changed.
  - `implementation_prep`: no implementation planning or architecture mapping is requested.
  - `feature`: no product feature work is requested.
  - `security`: no security rules, secrets, auth, or entitlement implementation is being reviewed.
  - `architecture`: no system architecture or component boundary change is requested.
  - `unknown`: not applicable because the user declared the class.
- Whether user-declared or Codex-inferred: user-declared.

## Plan Scope

- Allowed planning files/paths:
  - `tools/agent-review/**`
  - `.claude/settings.json`
  - `AGENTS.md`
  - `CLAUDE.md`

- Excluded planning files/paths:
  - PRD/PDD files, `docs/pdd/**`, `docs/submissions/**`
  - Flutter, Firebase, tests, diagrams, wireframes, images, PDFs, generated assets
  - `implementation/mobile/**`

- Large or expensive assets to avoid:
  - PDFs, images, diagrams, generated assets, submitted assessment files, build outputs, test evidence.

- Explicit Allow paths from the user:
  - `tools/agent-review/**`
  - `.claude/settings.json`
  - `AGENTS.md`
  - `CLAUDE.md`

- Escalation triggers:
  - Any need to inspect PRD/PDD or implementation files.
  - Any need for more than 3 representative files in lite review.
  - Any Review Scope file outside Plan Scope.
  - Any request to run actual pipeline commands, tests, builds, staging, commits, Flutter, Firebase, npm, or deployment.

## Review Scope

- Files expected to change:
  - None. Inspect-only smoke-test plan.

- Files Claude may need to read for review:
  - `tools/agent-review/README.md`
  - `tools/agent-review/runner/run_plan_review.sh`
  - `.claude/settings.json`

- Files explicitly out of scope:
  - Product requirements, submitted docs, PDD files, Flutter/Firebase source, tests, diagrams, wireframes, images, PDFs, generated assets, `implementation/mobile/**`.

- Risk tags:
  - touches XP/streak/level/rank/leaderboard: no
  - touches Firebase ownership or Cloud Functions ownership: no
  - touches roles/entitlements/premium fairness: no
  - touches security rules: no
  - touches PRD/PDD consistency: no
  - touches production source code: no

- Recommended review mode: lite

Review Scope consistency check: pass. All review files are inside Plan Scope or explicit Allow paths.

## Proposed Plan

- Confirm the workflow stays in `workflow` context and does not expand into product or implementation files.
- Verify the plan output includes compact `Planning Evidence Read`, `Context Class Decision`, `Plan Scope`, and minimal `Review Scope`.
- Verify lite review uses only representative workflow files and does not exceed 3 review files.
- Verify the runner supports `REVIEW_MODE=lite`, `CLAUDE_MAX_TURNS`, and `CLAUDE_MAX_BUDGET_USD`.
- Verify pipeline boundaries remain plan → review → decision only, with no implementation, tests, builds, staging, commits, Flutter, Firebase, npm, or deploy commands.
- If Claude needs more files or broader validation, return `DEFER` and recommend standard review or explicit Allow paths.

## Files Affected If Executed

- None for this inspect-only planning task.
- If a later approved actual pipeline run uses `DRY_RUN=0`, it may create plan/review/decision artifacts under configured traceability output directories, but that is outside this task.

## Files That Must Remain Untouched

- PRD/PDD files and submitted assessment files.
- Flutter, Firebase, tests, diagrams, wireframes, images, PDFs, generated assets.
- Production source code.
- Git index and commits.
- Any secrets or environment files.

## Risks

- Lite review may miss issues in profile prompts if they are not included in the representative file set.
- Runner behavior depends partly on external `codex` and `claude` CLI compatibility.
- `.claude/settings.json` denies many sensitive/large reads, but the plan should still rely on explicit scope control rather than broad reviewer discretion.

## Approval Gate

Ready for scope-limited lite review only. No implementation, command execution, file changes, staging, commits, tests, builds, Flutter, Firebase, npm, or deployment should occur without a separate explicit approval.
