## Goal

Create an inspect-only workflow smoke-test plan for `build_context_packet.sh` integration with `run_plan_review.sh` when `CONTEXT_PACKET_ENABLED=1`. No files should be modified, created, deleted, staged, committed, tested, built, deployed, or production-affecting commands run.

## Repo Context Checked

- A0_ORCH used as workflow owner.
- Cheap inventory only: root instruction discovery and `git status --short`.
- Current repo status is not clean: one unrelated untracked plan file exists at `implementation/traceability/plans/2026-05-24T06-49-35_codex_plan.md`.
- No broad repository scan performed.

## Planning Evidence Read

- Files actually read:
  - `AGENTS.md`
  - `tools/agent-review/runner/build_context_packet.sh`
  - `tools/agent-review/runner/run_plan_review.sh`
- Additional same-scope evidence summarized by category: root Claude review instruction, workflow context policy/settings, workflow README, and Codex plan prompt.
- Files intentionally skipped:
  - Product docs, PRD/PDD content, diagrams, generated assets, Flutter/Firebase source, tests, test evidence, PDFs, images, secrets, `.env*`.
- Reason for skipping large/reference/irrelevant files:
  - Task is a workflow smoke test, user-declared `workflow`; product and implementation files are outside scope and would broaden context unnecessarily.

## Context Class Decision

- Selected context class: `workflow`
- Reason: User explicitly declared `workflow`, and the task concerns context packet generation and runner integration.
- Excluded classes and why:
  - `docs`: not a documentation-content task.
  - `implementation_prep`: not planning app implementation.
  - `feature`: no product feature behavior requested.
  - `security`: no security-rule/auth/secret handling change requested.
  - `architecture`: no system/data/deployment architecture change requested.
  - `unknown`: not applicable because class was declared.
- Whether user-declared or Codex-inferred: user-declared.

## Plan Scope

- Allowed planning files/paths:
  - `AGENTS.md`
  - `CLAUDE.md`
  - `tools/agent-review/**`
  - `.claude/settings.json`
- Excluded planning files/paths:
  - Product requirements, submitted assessment docs, `docs/pdd/**`, diagrams, generated assets, Flutter/Firebase source, tests, test evidence, `.env*`, `secrets/**`.
- Large or expensive assets to avoid:
  - `docs/submissions/**`, `**/*.pdf`, `**/*.png`, `**/*.jpg`, `**/*.jpeg`, `**/*.svg`, `**/node_modules/**`, `**/build/**`, `**/.dart_tool/**`.
- Explicit Allow paths from the user, if any:
  - None beyond the provided workflow scope.
- Escalation triggers:
  - Need to inspect more than representative workflow files.
  - Need to validate product-requirement alignment.
  - Need to write smoke-test artifacts.
  - Context packet exceeds configured byte limit.
  - Review Scope requires files outside allowed workflow paths.

## Review Scope

- Files expected to change:
  - None. This is inspect-only.
- Files Claude may need to read for review:
  - `tools/agent-review/runner/build_context_packet.sh`
  - `tools/agent-review/runner/run_plan_review.sh`
  - `tools/agent-review/profiles/runiac/context-policy.yml`
- Files explicitly out of scope:
  - Product docs, PDD/PRD content, implementation source, Firebase source, tests, generated assets, submissions, secrets, `.env*`.
- Risk tags:
  - touches XP/streak/level/rank/leaderboard: no
  - touches Firebase ownership or Cloud Functions ownership: no
  - touches roles/entitlements/premium fairness: no
  - touches security rules: no
  - touches PRD/PDD consistency: no
  - touches production source code: no
- Recommended review mode: lite

## Proposed Plan

1. Confirm `workflow` context from the provided packet and do not infer broader product scope.
2. Inspect only representative workflow files needed to evaluate the smoke-test path.
3. Verify the intended smoke-test behavior at plan level:
   - `CONTEXT_PACKET_ENABLED=1` causes `run_plan_review.sh` to call `build_context_packet.sh`.
   - The generated packet is prepended to the Codex planning prompt.
   - Packet size is checked against `CONTEXT_PACKET_MAX_BYTES`.
   - Failure to generate the packet stops the pipeline without broad repo fallback.
4. Keep review limited to the three workflow files listed above.
5. Stop at inspect-only approval gate; do not run the smoke test or write artifacts.

## Files Affected If Executed

None for this inspect-only plan.

If a future approved smoke test is executed with `DRY_RUN=0`, it may create plan/review/decision artifacts under `implementation/traceability/**`, but that is outside this current request.

## Files That Must Remain Untouched

- `implementation/**`
- `firebase/**`
- `tests/**`
- `test-evidence/**`
- `docs/pdd/**`
- `docs/submissions/**`
- `.env*`
- `secrets/**`
- Existing unrelated untracked file: `implementation/traceability/plans/2026-05-24T06-49-35_codex_plan.md`

## Risks

- Current packet text says git status is clean, but actual `git status --short` shows one unrelated untracked file.
- Running `DRY_RUN=0` later could create traceability artifacts, so execution needs explicit approval.
- Lite review is appropriate only while scope remains workflow-only and inspect-only.

## Approval Gate

Ready for review of this inspect-only plan. No implementation, smoke-test execution, file writes, staging, commits, builds, tests, Flutter, Firebase, npm, or deployment were performed.
