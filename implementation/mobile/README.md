## Purpose
Reserves the future Flutter mobile application area for Runiac.

## Future contents
`runiac_app/` will be created later by running `flutter create runiac_app` from this folder.

## Scaffold-only note
No Flutter project, `pubspec.yaml`, `lib/`, `test/`, or `integration_test/` exists yet.
