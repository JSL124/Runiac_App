{
  "name": "runiac-firebase-rules-tests",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "cd ../.. && ./tests/firebase-rules/node_modules/.bin/firebase emulators:exec --only firestore \"cd tests/firebase-rules && node --test firestore.rules.test.mjs\""
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "3.0.4",
    "firebase": "10.14.1",
    "firebase-tools": "14.27.0"
  }
}
