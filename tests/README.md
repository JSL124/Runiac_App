## Purpose
Root-level `tests/` is for cross-system tests only.

## Future contents
Firebase rules tests, Functions integration tests, e2e scenarios, shared fixtures, and future test harness orchestration.

## Scaffold-only note
Flutter unit, widget, and integration tests must later live inside the Flutter app root.
